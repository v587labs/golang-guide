# 综合技术面试题库

**生成日期**: 2026-04-08

**技术板块**: Golang、Docker、Kubernetes、Linux、区块链、网络工程、PostgreSQL、Redis、AI辅助开发

**总题目数**: 170+

## 目录

1. [Golang 面试题](#1-golang-面试题)
2. [Docker 面试题](#2-docker-面试题)
3. [Kubernetes 面试题](#3-kubernetes-面试题)
4. [Linux 面试题](#4-linux-面试题)
5. [区块链 面试题](#5-区块链-面试题)
6. [计算机网络 面试题](#6-计算机网络-面试题)
7. [PostgreSQL 数据库面试题](#7-postgresql-数据库面试题)
8. [Redis 面试题](#8-redis-面试题)
9. [AI 辅助开发面试题](#9-ai-辅助开发面试题)

## 1. Golang 面试题

### 1.1 基础与语言特性

**题目 1**: Go语言是否支持面向对象编程？它如何实现面向对象的三大特性（封装、继承、多态）？

**答案**: Go语言通过不同的方式实现OOP特性：封装通过首字母大小写控制访问权限；继承通过结构体匿名组合实现；多态通过接口实现。

**题目 2**: Golang中make和new有什么区别？

**答案**: new给string、int和数组分配内存，返回指向变量的指针；make给切片、map、channel分配内存，返回变量本身；new分配的空间被清零，make会进行初始化。

**题目 3**: Go语言的select语句底层数据结构是什么？有哪些特性？

**答案**: select底层基于case语句和执行函数组成，使用链表管理各case；特性包括：至少有一个case操作管道、仅支持单协程操作管道、多个case执行顺序随机、存在default会使select不阻塞。

**题目 4**: defer的底层数据结构是什么？执行顺序是什么？

**答案**: defer对应一个_defer实例，多个实例用指针连接成单链表保存在goroutine中；执行顺序是后进先出（LIFO），先出现的defer最后执行。

**题目 5**: 单引号、双引号、反引号在Go中有什么区别？

**答案**: 单引号表示byte或rune类型（uint8或int32）；双引号表示字符串，是字节数组；反引号表示原始字符串字面量，不支持转义序列。

**题目 6**: Go语言中goroutine什么情况下会阻塞？

**答案**: 四种场景会导致阻塞：原子/互斥量/通道操作阻塞、网络请求和IO操作阻塞、系统方法调用阻塞、sleep操作阻塞。调度器会将阻塞的goroutine切换出去重新调度。

**题目 7**: Go中的协程与线程有什么区别？

**答案**: 协程是轻量级用户态线程，由Go运行时管理，创建成本极低（几千字节）；线程由操作系统管理，创建成本高；协程使用GPM调度模型，可以绑定到多个线程上运行。

**题目 8**: 讲讲Go的GMP调度模型？

**答案**: G（Goroutine）、M（Machine/线程）、P（Processor/处理器）是核心组件；P维护本地运行队列，G绑定到P执行，M关联P运行；通过工作窃取机制实现负载均衡。

**题目 9**: Go的垃圾回收机制是怎样的？

**答案**: Go采用三色标记清除算法，分为标记准备、标记、清除三个阶段；使用写屏障确保并发正确性；STW时间已优化到亚毫秒级别。

**题目 10**: 数组和切片有什么区别？

**答案**: 数组长度固定，值类型传递（深拷贝）；切片是引用类型，长度可变，底层指向数组；切片通过make或slice字面量创建。

**题目 11**: Map的实现原理是什么？并发访问需要注意什么？

**答案**: Map底层是hmap结构，通过哈希表实现；Go1.9后提供sync.Map实现并发安全；普通map并发读写会panic，需要使用锁或sync.Map。

**题目 12**: Go的内存逃逸分析是什么？

**答案**: 编译器通过逃逸分析决定变量分配在栈还是堆上；堆上分配会产生GC压力，栈上分配自动回收；原则是：变量在函数外部引用则逃逸到堆。

**题目 13**: Channel的底层实现是什么？无缓冲和缓冲channel有什么区别？

**答案**: Channel底层由hchan结构体实现，包含环形队列、发送/接收索引、阻塞队列；无缓冲channel要求发送和接收同时准备好；缓冲channel有容量限制。

**题目 14**: 互斥锁和读写锁有什么区别？分别在什么场景使用？

**答案**: 互斥锁 Lock/Unlock 成对使用；读写锁适合读多写少场景，读锁可以并发获取，写锁独占；场景：配置更新用互斥锁，缓存读取用读写锁。

**题目 15**: for range遍历切片时需要注意什么问题？

**答案**: for range中变量a和b的地址始终不变，每次循环是值覆盖；在循环内开协程时不要直接传a或b的地址，应创建临时变量。

**题目 16**: Go中如何实现类似继承的机制？

**答案**: Go通过匿名组合实现继承：结构体嵌套匿名结构体可以直接访问其属性和方法；嵌套多个匿名结构体可实现多重继承。

### 1.2 进阶与实战

**题目 17**: context包的作用是什么？使用场景有哪些？

**答案**: Context用于在Goroutine之间传递截止时间、取消信号和请求域；使用场景：超时控制、取消执行链路、传递请求级别的值。

**题目 18**: Go单元测试有哪些规范？

**答案**: 测试文件命名为xxx_test.go；包名为原包名加_test后缀；测试方法以Test开头并接受*testing.T参数；使用t.Run进行子测试。

**题目 19**: 如何避免goroutine泄漏？

**答案**: 使用context的cancel、timeout或done channel通知goroutine退出；确保每个goroutine都有对应的退出机制；使用sync.WaitGroup跟踪goroutine完成。

**题目 20**: Go的错误处理机制是怎样的？为什么不推荐使用try-catch？

**答案**: Go通过返回error值处理错误，遵循显式错误处理原则；defer用于资源清理；不推荐panic用于常规错误控制，因为会影响调用栈。

## 2. Docker 面试题

### 2.1 基础概念

**题目 1**: 什么是Docker？它与虚拟机的区别是什么？

**答案**: Docker是容器化平台，基于Linux内核的CGroup和namespace实现进程隔离；虚拟机虚拟化硬件并运行完整操作系统，容器共享宿主机内核，更轻量。

**题目 2**: Docker的三大核心概念是什么？

**答案**: 镜像（Image）是只读模板，包含程序和运行环境；容器（Container）是镜像运行实体；仓库（Repository）集中存储和分发镜像。

**题目 3**: Docker镜像的分层存储原理是什么？有什么优势？

**答案**: Docker使用Union FS实现分层构建，每层构建完不再改变；优势是复用和共享、构建高效、存储节约；容器层记录变更。

**题目 4**: 容器和镜像的关系是什么？

**答案**: 镜像是静态定义，容器是镜像运行时的实体；容器创建时在镜像层上添加可写层；删除容器不影响镜像。

**题目 5**: Dockerfile中COPY和ADD指令有什么区别？

**答案**: COPY仅复制文件或目录；ADD支持URL下载和tar解压；在大多数场景下推荐使用COPY，语义更清晰。

**题目 6**: CMD和ENTRYPOINT指令有什么区别？

**答案**: CMD提供容器启动默认命令，可被docker run参数覆盖；ENTRYPOINT设置不可变的启动命令；两者结合可用于构建可配置容器。

**题目 7**: 多阶段构建镜像的优势是什么？如何使用？

**答案**: 多阶段构建可以分离编译环境和运行环境，减小最终镜像体积；使用AS给编译阶段命名，从前一阶段复制产物到后一阶段。

**题目 8**: Docker的网络模式有哪些？各自特点是什么？

**答案**: bridge模式创建独立网络栈；host模式共享宿主机网络；container模式共享其他容器网络；none模式禁用网络。

**题目 9**: 数据卷（Volume）的作用是什么？有哪几种类型？

**答案**: 数据卷用于持久化数据，绕过容器存储层；有命名卷、绑定挂载、tmpfs挂载三种类型；推荐使用命名卷便于管理。

**题目 10**: 如何优化Docker镜像大小？

**答案**: 使用多阶段构建；选择合适的基础镜像（如alpine）；减少镜像层数；清理缓存和临时文件；使用.dockerignore排除不需要文件。

**题目 11**: Docker Compose的作用是什么？如何定义服务？

**答案**: Compose用于定义和运行多容器应用；通过YAML文件定义services、networks、volumes；使用docker-compose up一键启动。

**题目 12**: 容器与虚拟机相比有哪些优势和劣势？

**答案**: 优势：启动快速、占用资源少、移植性强、隔离性好；劣势：隔离级别低于虚拟机、共享内核安全性略低、需要容器运行时支持。

### 2.2 进阶与实践

**题目 13**: Docker的存储驱动有哪些？如何选择？

**答案**: 常见存储驱动包括overlay2、devicemapper、btrfs、zfs；推荐生产环境使用overlay2，它性能好且兼容性广。

**题目 14**: 如何实现Docker容器健康检查？

**答案**: 在Dockerfile中使用HEALTHCHECK指令定义检查命令；或使用docker-compose的healthcheck配置；支持CMD、HTTP、TCP三种检查方式。

**题目 15**: Docker的资源限制如何配置？

**答案**: 使用--memory限制内存、--cpus限制CPU；可设置--memory-reservation软限制；--blkio-weight限制块IO权重。

**题目 16**: 如何清理Docker占用的资源？

**答案**: docker system prune清理未使用的镜像、容器、网络；docker volume prune清理未使用的卷；定期清理可释放磁盘空间。

## 3. Kubernetes 面试题

### 3.1 架构与核心概念

**题目 1**: Kubernetes是什么？它的核心功能有哪些？

**答案**: Kubernetes是容器编排平台，源自Google的Borg系统；核心功能包括：容器编排、自动调度、自我修复、水平扩展、服务发现、配置管理、存储编排。

**题目 2**: Kubernetes集群的核心组件有哪些？

**答案**: Master节点组件：kube-apiserver（API入口）、kube-controller-manager（控制器管理）、kube-scheduler（调度器）、etcd（存储）；Node节点组件：kubelet、kube-proxy、container runtime。

**题目 3**: Pod是什么？Pod有哪些状态？

**答案**: Pod是K8s最小调度单位，包含一个或多个共享网络/存储的容器；状态包括：Pending（等待调度）、Running（运行中）、Succeeded（成功结束）、Failed（失败）、Unknown（未知）。

**题目 4**: ReplicaSet和Replication Controller有什么区别？

**答案**: 功能相同，都是保证Pod副本数量；区别在于RS使用基于集合的选择器（set-based selector），RC使用基于等值的选择器（equality-based selector）；Deployment内部使用RS。

**题目 5**: Deployment的升级策略有哪些？如何回滚？

**答案**: 升级策略有Recreate（重建）和RollingUpdate（滚动更新）；RollingUpdate通过maxSurge和maxUnavailable控制节奏；使用kubectl rollout undo回滚到历史版本。

**题目 6**: Service的类型有哪些？各自用途是什么？

**答案**: ClusterIP（集群内部访问）、NodePort（通过节点端口访问）、LoadBalancer（云负载均衡器）、ExternalName（DNS别名）；Headless Service不分配ClusterIP，直接返回Pod IP。

**题目 7**: 什么是Ingress？它与Service的区别是什么？

**答案**: Ingress提供HTTP层路由规则，基于域名和路径转发到后端Service；Service是四层代理，Ingress是七层代理；Ingress需要Ingress Controller配合使用。

**题目 8**: 什么是ConfigMap和Secret？如何使用？

**答案**: ConfigMap存储配置数据，Secret存储敏感信息（如密码、密钥）；使用方式：环境变量注入、命令行参数、卷挂载。

**题目 9**: 什么是PV和PVC？它们的关系是什么？

**答案**: PV（PersistentVolume）是集群级存储资源；PVC（PersistentVolumeClaim）是用户对存储的申请；PVC绑定到满足条件的PV，实现存储解耦。

**题目 10**: 什么是Namespace？它的作用是什么？

**答案**: Namespace实现资源隔离，将集群划分为多个虚拟集群；用于多租户隔离、区分不同环境（dev/staging/prod）、资源配额管理。

### 3.2 调度与网络

**题目 11**: Kubernetes的调度流程是什么？

**答案**: 调度流程包括：预选（Predicates）过滤不符合条件的节点、优选（Priorities）对通过的节点打分、选择得分最高的节点绑定Pod。

**题目 12**: Pod调度方式有哪些？

**答案**: Deployment/RC自动调度、NodeSelector节点选择、NodeAffinity节点亲和性、PodAffinity/PodAntiAffinity Pod亲和性、Taints/Tolerations污点与容忍。

**题目 13**: Kubernetes的网络模型是怎样的？

**答案**: 每个Pod拥有唯一IP，Pod间可直接通信；同一Pod内容器共享网络命名空间；Service提供稳定的虚拟IP做负载均衡；CNI插件负责Pod网络配置。

**题目 14**: kube-proxy的工作模式有哪些？有什么区别？

**答案**: userspace模式（代理模式）、iptables模式（规则转发）、IPVS模式（哈希表转发）；IPVS性能最好，支持更多负载均衡算法。

**题目 15**: 常见的CNI插件有哪些？各自特点是什么？

**答案**: Flannel（简单Overlay网络）、Calico（高性能三层网络，支持网络策略）、Weave（Overlay网络）、Cilium（eBPF加速，网络策略）。

**题目 16**: 什么是NetworkPolicy？如何使用？

**答案**: NetworkPolicy定义Pod间网络访问规则，实现细粒度网络隔离；规则包括入口/出口规则、标签选择器、端口规范；需要CNI插件支持。

### 3.3 运维与安全

**题目 17**: Pod的重启策略有哪些？

**答案**: Always（容器失效自动重启，默认值）、OnFailure（仅在异常退出时重启）、Never（不重启）；不同控制器对重启策略有要求。

**题目 18**: Pod的健康检查方式有哪些？

**答案**: LivenessProbe（存活探针，判断是否需要重启容器）、ReadinessProbe（就绪探针，判断是否接收流量）、StartupProbe（启动探针，用于慢启动应用）。

**题目 19**: 如何实现Pod自动扩缩容？

**答案**: 使用HPA（Horizontal Pod Autoscaler）基于CPU/内存指标自动调整Pod副本数；K8s 1.9+支持自定义指标HPA；需要Metrics Server提供指标数据。

**题目 20**: Kubernetes的安全机制有哪些？

**答案**: 认证（Authentication）：API Server验证客户端身份；授权（Authorization）：RBAC基于角色权限控制；准入控制（AdmissionControl）：对请求进行二次验证；Secret加密存储。

**题目 21**: 什么是RBAC？如何使用？

**答案**: RBAC（基于角色的访问控制）通过Role/RoleBinding或ClusterRole/ClusterRoleBinding管理权限；Role定义权限规则，RoleBinding绑定到Subject（用户/组/SA）。

**题目 22**: 如何进行优雅的节点维护？

**答案**: 使用kubectl drain安全驱逐节点上Pod；设置--ignore-daemonsets和--delete-emptydir-data参数；维护完成后使用kubectl uncordon恢复调度。

**题目 23**: Helm是什么？有什么作用？

**答案**: Helm是K8s的包管理工具，类似于yum/apt；Chart是打包格式，包含YAML模板和依赖关系；用于简化应用部署、升级、回滚。

**题目 24**: 如何排查Pod启动失败的问题？

**答案**: 使用kubectl describe查看事件和状态；使用kubectl logs查看容器日志；检查镜像是否存在、端口是否冲突、资源是否充足、配置是否正确。

## 4. Linux 面试题

### 4.1 基础命令

**题目 1**: 如何查看Linux系统进程？有哪些常用命令？

**答案**: ps -ef显示所有进程完整格式；ps aux显示包含其他用户的行程；top命令实时查看CPU和内存使用情况，load average显示系统负载。

**题目 2**: 如何查找文件？find和grep有什么区别？

**答案**: find用于查找文件，支持-name、-type、-size等条件；grep用于查找文件内容，支持正则表达式匹配；find查找文件本身，grep查找文本内容。

**题目 3**: 如何查看日志文件？tail和cat有什么区别？

**答案**: cat查看整个文件内容；more/less分页查看；tail -f动态查看新增内容；tail -n指定行数查看；head查看文件开头。

**题目 4**: 如何修改文件权限？chmod的数字权限如何计算？

**答案**: chmod使用数字或符号修改权限；数字表示：r=4、w=2、x=1；例如755表示rwxr-xr-x（所有者全部权限，其他人读和执行）。

**题目 5**: 如何查看端口占用情况？

**答案**: netstat -anp | grep端口号查看端口和进程；lsof -i:端口号查看占用端口的进程；ss -tlnp更高效查看监听端口。

**题目 6**: 如何杀掉进程？kill和killall有什么区别？

**答案**: kill -9强制终止进程；kill -15正常终止；killall按进程名杀进程；pkill支持进程名模式匹配。

**题目 7**: df和du命令有什么区别？

**答案**: df查看文件系统磁盘空间使用情况；du统计文件或目录占用空间大小；du -sh /*查看根目录下各目录大小。

**题目 8**: 如何查看系统资源使用情况？

**答案**: top查看CPU、内存、进程信息；vmstat查看虚拟内存统计；iostat查看IO统计；free -h查看内存使用。

### 4.2 系统管理

**题目 9**: Linux开机启动流程是什么？

**答案**: BIOS/UEFI自检→引导加载程序（GRUB）→加载内核→启动systemd初始化进程→根据target启动服务→用户登录。

**题目 10**: systemd和init的区别是什么？

**答案**: init是传统的SysVinit串行启动，速度慢；systemd并行启动服务，支持依赖管理、单元文件、日志管理；使用systemctl管理服务。

**题目 11**: 如何管理系统服务？

**答案**: systemctl start/stop/restart服务名；systemctl enable/disable开机自启；systemctl status查看状态；systemctl daemon-reload重载配置。

**题目 12**: 如何配置cron定时任务？

**答案**: crontab -e编辑定时任务；格式：分 时 日 月 周 命令；*表示任意；使用crontab -l查看任务；日志通常在/var/log/cron。

**题目 13**: 软链接和硬链接有什么区别？

**答案**: 软链接是符号链接，指向文件路径，可跨文件系统；硬链接指向inode，同一文件多个名字，不能跨文件系统，不能链接目录。

**题目 14**: 如何查看网络连接和路由信息？

**答案**: netstat/ss查看网络连接；ip addr show查看IP地址；ip route show查看路由表；traceroute追踪路由路径。

**题目 15**: 如何压缩和解压文件？

**答案**: tar -czvf打包压缩；tar -xzvf解压；zip/unzip处理zip文件；tar支持.gz、.bz2、.xz等多种格式。

**题目 16**: 如何查看系统负载和性能瓶颈？

**答案**: uptime查看负载均值；top定位CPU/内存高占用进程；iostat定位IO瓶颈；free查看内存使用；sar收集系统性能数据。

### 4.3 进阶操作

**题目 17**: Shell脚本中如何进行错误处理？

**答案**: set -e遇错误退出；set -u使用未定义变量报错；set -o pipefail管道失败返回最后命令状态；trap捕获信号进行清理。

**题目 18**: 如何排查网络问题？

**答案**: ping检测连通性；telnet/nc检测端口；curl测试HTTP服务；tcpdump抓包分析；netstat/ss查看连接状态；traceroute追踪路由。

**题目 19**: 什么是IO多路复用？Linux支持哪些机制？

**答案**: IO多路复用允许单线程同时监控多个IO事件；select/poll/epoll是Linux实现；epoll使用事件通知，性能最高，支持边缘触发和水平触发。

**题目 20**: 如何查看系统日志？日志存放位置在哪里？

**答案**: /var/log/messages系统消息；/var/log/secure安全日志；/var/log/nginx/应用日志；journalctl查看systemd日志；使用tail -f实时监控。

## 5. 区块链 面试题

### 5.1 基础概念

**题目 1**: 什么是区块链？它的核心特征是什么？

**答案**: 区块链是分布式账本技术，通过密码学链接数据块形成不可变记录；核心特征：去中心化、不可篡改、可追溯、透明公开。

**题目 2**: 区块链有哪些类型？有什么区别？

**答案**: 公链（Public Blockchain）公开访问，如比特币、以太坊；私链（Private Blockchain）限制参与，如Hyperledger；联盟链（Consortium Blockchain）多方共识，如R3 Corda。

**题目 3**: 什么是共识算法？为什么区块链需要共识算法？

**答案**: 共识算法让分布式节点达成状态一致；区块链无中心节点，需共识保证数据一致性；常见算法：PoW、PoS、DPoS、Raft、Paxos。

**题目 4**: PoW（工作量证明）是如何工作的？

**答案**: 矿工竞争计算哈希难题，找到满足条件的随机数；算力决定概率，保证51%攻击成本高；比特币使用SHA256算法。

**题目 5**: PoS（权益证明）与PoW有什么区别？

**答案**: PoW依赖算力，消耗能源；PoS根据持币量和时间选择验证者，降低能耗；PoS存在"富者愈富"问题，通常结合惩罚机制。

**题目 6**: 什么是51%攻击？它有什么危害？

**答案**: 控制超过50%算力/权益可以操纵区块链；可导致双花、阻止确认交易、 censorship；理论上PoW难以攻击，PoS风险相对较高。

**题目 7**: 什么是区块头哈希？如何保证链的连续性？

**答案**: 区块头哈希由前一区块哈希、默克尔根、时间戳等计算生成；每个区块包含前一区块哈希，形成链式结构；篡改历史需要重算所有后续区块。

**题目 8**: 什么是默克尔树？它在区块链中的作用是什么？

**答案**: 默克尔树是哈希树结构，叶子是交易哈希，逐层向上哈希合并；可快速验证交易存在性；简化支付验证（SPV）的基础。

**题目 9**: 什么是智能合约？以太坊的智能合约有什么特点？

**答案**: 智能合约是部署在区块链上的自动化合约代码；以太坊使用Solidity语言，支持图灵完备编程；部署后不可更改，自动执行。

**题目 10**: 什么是双花问题？比特币如何解决？

**答案**: 双花指同一笔资金使用两次；比特币通过时间戳和最长链原则解决；交易确认数越多，双花概率越低。

### 5.2 技术细节

**题目 11**: 什么是隔离见证（SegWit）？它解决了什么问题？

**答案**: SegWit将签名数据从交易中分离；解决比特币交易延展性问题；增加区块有效容量；为闪电网络奠定基础。

**题目 12**: 什么是闪电网络？它的工作原理是什么？

**答案**: 闪电网络是比特币的二层扩容方案；在链下建立支付通道，多笔交易链下完成；最终结算才上链，提高交易速度并降低费用。

**题目 13**: 什么是软分叉和硬分叉？有什么区别？

**答案**: 软分叉保持向后兼容，旧节点仍能验证新规则区块；硬分叉不兼容，旧版本无法接受新区块；ETH/ETC是典型硬分叉案例。

**题目 14**: 区块链中的加密算法有哪些？

**答案**: 哈希算法：SHA256（比特币）、Keccak256（以太坊）；非对称加密：ECDSA（比特币）、Ed25519；默克尔树：验证数据完整性。

**题目 15**: 什么是预言机（Oracle）？为什么需要它？

**答案**: 区块链无法直接获取外部数据；预言机将链外数据安全引入链上；用于DeFi价格喂价、保险理赔、随机数等场景。

**题目 16**: 什么是以太坊虚拟机（EVM）？它有什么特点？

**答案**: EVM是运行智能合约的图灵完备虚拟机；每个操作都有固定的Gas消耗；沙箱环境确保安全性；支持多种编程语言编译。

**题目 17**: 什么是Gas？为什么需要Gas？

**答案**: Gas是执行操作的燃料，衡量计算工作量；防止无限循环攻击；执行失败Gas不退款；Gas价格由市场决定。

**题目 18**: 什么是零知识证明？有什么应用场景？

**答案**: 零知识证明在不泄露信息内容的情况下证明真实性；应用场景：隐私交易（Zcash）、身份验证、可扩展性解决方案（ZK-Rollup）。

### 5.3 应用与安全

**题目 19**: 常见的智能合约安全漏洞有哪些？

**答案**: 重入攻击（DAO事件）、整数溢出、未经授权的访问、拒绝服务、前端运行攻击；需要安全审计和形式化验证。

**题目 20**: 什么是DeFi？它与传统金融有什么区别？

**答案**: DeFi是去中心化金融，基于区块链和智能合约；无需许可、公开透明、无单点故障；通过AMM、借贷协议、衍生品实现金融功能。

**题目 21**: 什么是NFT？它有哪些应用场景？

**答案**: NFT（非同质化代币）代表独特资产；应用场景：数字艺术、游戏道具、域名、音乐版权、身份凭证；通过ERC-721标准实现。

**题目 22**: 区块链的扩展性解决方案有哪些？

**答案**: Layer1扩展：增加区块大小、分片；Layer2扩展：闪电网络、Plasma、Rollup；侧链：Polygon、Ronin；各有权衡取舍。

**题目 23**: 什么是Web3？它与Web2有什么区别？

**答案**: Web3是去中心化互联网概念；Web2平台依赖、数据由公司控制；Web3用户拥有数据和身份、使用区块链进行价值转移、通过钱包连接。

**题目 24**: 区块链在供应链管理中如何应用？

**答案**: 全流程可追溯，防伪鉴真；智能合约自动化流程；多参与方数据共享；降低欺诈风险；提升供应链透明度。

## 6. 计算机网络 面试题

### 6.1 网络基础与协议

**题目 1**: 请简述OSI七层模型和TCP/IP四层模型的区别与对应关系。

**答案**: OSI七层模型是理论模型，从下到上包括物理层、数据链路层、网络层、传输层、会话层、表示层、应用层；TCP/IP四层模型是实际应用模型，包括网络接口层、网络层（IP）、传输层（TCP/UDP）、应用层；对应关系：应用层对应OSI的应用层、表示层、会话层；传输层对应传输层；网络层对应网络层；网络接口层对应物理层和数据链路层。

**题目 2**: TCP和UDP的区别是什么？各自适用于哪些场景？

**答案**: TCP是面向连接、可靠的字节流服务，通过三次握手建立连接、四次挥手断开连接，提供重传、流量控制、拥塞控制；UDP是无连接的、不可靠的报文服务，不保证交付但传输效率高。TCP适用于HTTP/HTTPS、FTP、SMTP等需要可靠传输的场景；UDP适用于实时音视频、DNS查询、 DHCP、游戏等对实时性要求高的场景。

**题目 3**: 为什么TCP是三次握手而不是两次？

**答案**: 两次握手无法阻止历史重复连接初始化；三次握手可以同步双方的序列号，确认双方发送和接收能力正常；客户端超时重传可以处理历史连接问题；第三次握手还可以携带数据，防止恶意攻击。

**题目 4**: TCP四次挥手为什么要等待2MSL？

**答案**: MSL是最大报文生存时间；等待2MSL确保被动关闭方的FIN到达并处理，防止旧连接报文影响新连接；确保本次连接所有报文在网络中消失，防止影响下一个连接；客户端和服务器都需要进入时间等待状态。

**题目 5**: 什么是滑动窗口协议？它解决了什么问题？

**答案**: 滑动窗口是TCP流量控制机制，发送方维护一个窗口大小，表示无需等待确认即可发送的数据量；窗口大小由接收方通告；解决了网络传输中发送方和接收方处理速度不匹配的问题，避免发送过快导致接收方缓冲区溢出。

**题目 6**: HTTP和HTTPS有什么区别？HTTPS的工作原理是什么？

**答案**: HTTP是明文传输协议，端口80；HTTPS使用TLS/SSL加密，端口443；HTTPS结合了对称加密（数据加密）、非对称加密（密钥交换）、数字证书（身份验证）；工作流程：客户端发起HTTPS请求→服务器发送证书→客户端验证证书→生成会话密钥→加密通信。

**题目 7**: HTTP的常见状态码有哪些？分别代表什么含义？

**答案**: 1xx表示信息性状态码（继续、处理中）；2xx成功（200 OK、201 Created、204 No Content）；3xx重定向（301永久移动、302临时移动、304 Not Modified）；4xx客户端错误（400 Bad Request、401 Unauthorized、403 Forbidden、404 Not Found）；5xx服务器错误（500 Internal Server Error、502 Bad Gateway、503 Service Unavailable、504 Gateway Timeout）。

**题目 8**: HTTP的GET和POST方法有什么区别？

**答案**: GET用于获取资源，参数通过URL传递，有长度限制（浏览器限制），会被缓存，适合查询；POST用于提交数据，参数放在请求体中，无长度限制，不被缓存，适合提交表单或上传文件；GET是幂等的，POST不是。

**题目 9**: 什么是DNS协议？它的工作原理是什么？

**答案**: DNS（域名系统）将域名解析为IP地址；工作原理：浏览器缓存→系统缓存→本地域名服务器→根域名服务器→顶级域名服务器→权威域名服务器；查询方式包括递归查询（客户端到本地域名服务器）和迭代查询（域名服务器之间）。

**题目 10**: 什么是ARP协议？它有什么作用？

**答案**: ARP（地址解析协议）将IP地址解析为MAC地址；工作过程：主机发送ARP请求广播（包含目标IP），目标主机响应ARP应答（包含MAC地址）；ARP缓存用于存储IP-MAC映射；ARP欺骗是常见网络攻击方式。

### 6.2 网络编程与进阶

**题目 11**: Socket编程中，服务器端有哪些主要步骤？

**答案**: 服务器端Socket编程主要步骤：创建Socket（socket()）、绑定地址端口（bind()）、监听连接（listen()）、接受连接（accept()）、收发数据（send()/recv()）、关闭连接（close()）；使用多进程或多线程处理多个客户端连接，或使用IO多路复用（select/poll/epoll）。

**题目 12**: 什么是IO多路复用？select、poll、epoll有什么区别？

**答案**: IO多路复用允许单线程同时监控多个文件描述符；select使用位图，有fd数量限制（默认1024），需要遍历所有fd；poll使用数组结构，无数量限制但仍需遍历；epoll使用红黑树和回调机制，只返回就绪的fd，效率最高，支持边缘触发和水平触发。

**题目 13**: 什么是三次握手和四次挥手？请详细描述过程。

**答案**: 三次握手：客户端发送SYN（seq=x）→服务器返回SYN+ACK（seq=y, ack=x+1）→客户端发送ACK（ack=y+1）；四次挥手：主动方发送FIN→被动方返回ACK→被动方发送FIN→主动方返回ACK；TIME_WAIT状态在主动关闭方保持2MSL。

**题目 14**: TCP的拥塞控制算法有哪些？

**答案**: 拥塞控制算法包括：慢启动（指数增长）、拥塞避免（线性增长）、快重传（收到三个重复ACK立即重传）、快恢复（进入拥塞避免而非慢启动）；常见算法：Tahoe（慢启动+快重传+快恢复）、Reno（ Tahoe+快速恢复）、NewReno（改进快恢复）、CUBIC（Linux默认）。

**题目 15**: 什么是Cookie和Session？它们有什么区别？

**答案**: Cookie是服务器发送到浏览器并存储在本地的小型文本文件，用于跟踪用户状态；Session是服务器端存储的用户会话信息，通过Session ID关联；Cookie存储在客户端，安全性较低但减轻服务器压力；Session存储在服务器端，安全性高但增加服务器内存开销。

**题目 16**: 什么是CDN？它的工作原理是什么？

**答案**: CDN（内容分发网络）是分布式服务器网络，缓存静态内容到离用户最近的节点；工作原理：用户请求资源→DNS解析到最近CDN节点→CDN节点返回缓存内容（未命中则回源获取）→返回给用户；优势：加速访问、减轻源站压力、提高可用性。

## 7. PostgreSQL 数据库面试题

### 7.1 基础与架构

**题目 1**: PostgreSQL是什么？它与MySQL有什么区别？

**答案**: PostgreSQL是开源对象关系型数据库，以稳定性、功能丰富著称；与MySQL的区别：PostgreSQL支持MVCC并发控制、复杂数据类型（JSON/数组/范围类型）、表继承、规则系统；MySQL在简单查询性能上更优，PostgreSQL在复杂查询和数据完整性上更强；PostgreSQL是完全ACID兼容的事务数据库。

**题目 2**: PostgreSQL的架构是怎样的？有哪些主要组件？

**答案**: PostgreSQL采用客户端-服务器架构；主要组件：Postmaster（主进程，负责监听连接）、Backend进程（每个连接一个）、Shared Memory（共享内存）、WAL Writer（预写日志）、Checkpointer（检查点进程）、Autovacuum（自动清理进程）；使用MVCC实现并发，支持多种存储引擎。

**题目 3**: PostgreSQL有哪些索引类型？各自的特点是什么？

**答案**: 索引类型：B-Tree索引（默认，适合等值和范围查询）、Hash索引（仅支持等值比较）、GiST索引（几何数据、全文搜索）、GIN索引（倒排索引，适合数组、全文搜索）、BRIN索引（块级索引，适合顺序大数据）、Partial索引（条件索引）；不同索引适用于不同场景。

**题目 4**: PostgreSQL的MVCC是如何实现的？有什么优势？

**答案**: PostgreSQL MVCC通过事务ID（xmin/xmax）和快照实现；每行数据有xmin（插入事务ID）和xmax（删除/更新事务ID）；读取时根据事务快照判断行的可见性；优势：读操作不阻塞写操作，写操作不阻塞读操作，提高并发性能；避免脏读和不可重复读。

**题目 5**: 什么是WAL（预写日志）？它有什么作用？

**答案**: WAL是Write-Ahead Logging，修改数据前先写入日志；作用：保证数据持久性（崩溃恢复）、支持物理复制和逻辑复制、确保事务原子性；比直接刷脏页更高效（日志顺序写）；通过配置wal_level控制详细程度（minimal/replica/logical）。

### 7.2 事务与并发

**题目 6**: PostgreSQL的事务隔离级别有哪些？与MySQL有什么区别？

**答案**: 隔离级别：Read Uncommitted（实际表现为Read Committed）、Read Committed（默认）、Repeatable Read、Serializable；与MySQL区别：MySQL默认Repeatable Read，PostgreSQL默认Read Committed；PostgreSQL的Serializable通过SSI（串行化快照）实现，性能优于传统锁方式；PostgreSQL不会出现MySQL的"幻读"问题。

**题目 7**: PostgreSQL的锁机制有哪些？与MySQL有什么不同？

**答案**: PostgreSQL锁类型：表级锁（Access Share、Share Row Exclusive等）、行级锁（FOR UPDATE/SHARE）、 advisory锁（应用自定义）；与MySQL区别：PostgreSQL使用MVCC，大部分读操作不加锁；InnoDB是行锁，但需配合gap锁；PostgreSQL显式使用行锁会阻塞其他行锁，MySQL不会。

**题目 8**: 什么是PostgreSQL的 VACUUM？为什么要进行VACUUM？

**答案**: VACUUM是清理已删除/更新行占用的磁盘空间；MVCC机制使旧版本数据成为"死元组"，需清理释放空间；VACUUM类型：普通VACUUM（可与读写并发）、VACUUM FULL（排他锁，整理表）；Autovacuum自动执行；大量更新后需手动VACUUM避免表膨胀。

**题目 9**: PostgreSQL如何处理死锁？

**答案**: PostgreSQL自动检测死锁（通过等待图）；检测到死锁后会回滚其中一个事务（通常是占用锁最少的事务）；应用程序应捕获 deadlock_detected 错误并重试；通过设置 deadlock_timeout 调整检测灵敏度；避免死锁：按固定顺序访问表。

### 7.3 性能与优化

**题目 10**: 如何优化PostgreSQL查询性能？

**答案**: 优化方法：使用EXPLAIN/EXPLAIN ANALYZE分析执行计划；创建合适索引（B-Tree/GIN/BRIN）；避免SELECT *，只查询需要的列；使用连接池（PgBouncer）；配置effective_cache_size和shared_buffers；定期执行VACUUM ANALYZE；使用分页优化大结果集。

**题目 11**: PostgreSQL的执行计划如何分析？

**答案**: 查看执行计划关键指标：Seq Scan（全表扫描，应避免大表）、Index Scan（索引扫描）、Bitmap Heap Scan（位图扫描）、Nested Loop/Hash Join/Merge Join（连接方式）；关注cost值（启动cost、总cost）、rows估计值与实际值对比；使用pg_stat_user_indexes查看索引使用情况。

**题目 12**: PostgreSQL的配置参数如何调优？

**答案**: 关键参数：shared_buffers（共享内存，推荐25%内存）、effective_cache_size（查询规划器缓存估计，50-75%内存）、work_mem（排序/哈希内存）、maintenance_work_mem（维护操作内存）、max_connections（最大连接数）、wal_buffers（WAL缓冲区）；根据服务器资源和负载调整。

### 7.4 高级特性与集群

**题目 13**: PostgreSQL的复制方式有哪些？如何实现高可用？

**答案**: 复制方式：流复制（物理复制，实时同步）、逻辑复制（基于WAL，可选择性复制）；流复制配置：主库设置wal_level=replica，备库使用pg_basebackup；高可用方案：Patroni+Etcd、Repmgr；支持一主多从、同步/异步复制；切换时使用pg_pool或Keepalived。

**题目 14**: PostgreSQL的分表分库方案有哪些？

**答案**: 分区表（Partitioning）：RANGE分区（按时间/ID范围）、LIST分区（按枚举值）、HASH分区；原生分区表减少索引维护；分库方案：PL/Proxy（应用层路由）、Citus（扩展支持分布式）、Pgpool-II（中间件）；需考虑分布式事务（2PC）和数据迁移。

**题目 15**: PostgreSQL有哪些高级数据类型？使用场景是什么？

**答案**: 高级类型：JSON/JSONB（半结构化数据，JSONB已解析更快）、数组类型（一列多值）、范围类型（时间范围、数值范围）、HSTORE（键值对）、UUID（通用唯一标识符）、PostGIS（地理空间数据）；适用场景：灵活数据模型、时序数据、地理信息、多值属性。

**题目 16**: PostgreSQL的全文搜索是如何实现的？

**答案**: 全文搜索使用tsvector和tsquery类型；配置：ALTER TABLE ADD COLUMN search_vector tsvector；创建GIN索引加速搜索；使用to_tsquery/to_tsvector函数；支持自定义词典和权重；支持25种语言的词干提取；配合ts_rank排序相关性。

**题目 17**: PostgreSQL的备份与恢复策略是什么？

**答案**: 备份方式：物理备份（pg_basebackup直接复制数据目录）、逻辑备份（pg_dump/pg_dumpall导出SQL）；恢复方式：PITR（时间点恢复，基于WAL重放）；连续归档配置：archive_mode=on；备份策略：每日全量+持续WAL归档；测试恢复流程确保数据安全。

## 8. Redis 面试题

### 8.1 基础与数据结构

**题目 1**: Redis是什么？它为什么这么快？

**答案**: Redis是开源的内存数据结构存储系统，可用作数据库、缓存、消息队列；快的三个原因：纯内存操作，没有磁盘IO开销；单线程模型（6.0前），避免上下文切换和锁竞争；IO多路复用，使用epoll处理并发请求。

**题目 2**: Redis有哪些主要的数据结构？

**答案**: Redis五种主要数据结构：String（字符串，SDS实现）、List（列表，双向链表或压缩列表）、Hash（哈希，压缩列表或哈希表）、Set（集合，哈希表或整数集合）、Sorted Set（有序集合，跳表或压缩列表）；还支持Bitmap、HyperLogLog、Geospatial等特殊类型。

**题目 3**: String类型的底层实现是什么？SDS相比C字符串有什么优势？

**答案**: String类型使用SDS（Simple Dynamic String）实现；SDS结构包含len（长度）、alloc（分配空间）、flags（类型）、buf[]（字符数组）；优势：O(1)获取长度、二进制安全（不依赖\0判断）、防止缓冲区溢出、节省内存（使用flags区分类型）。

**题目 4**: 什么是跳表？为什么Redis选择跳表而不是红黑树实现Sorted Set？

**答案**: 跳表是链表+多级索引实现，支持O(logn)查找；Redis选择跳表的原因：跳表区间查询方便（从某节点往后遍历），红黑树区间查询需中序遍历；跳表实现简单，调参灵活（调整索引层数）；内存占用相对较低，平衡查找效率和空间消耗。

**题目 5**: Redis的过期键删除策略有哪些？

**答案**: 三种删除策略：定时删除（创建定时器，到期删除，对CPU不友好）、惰性删除（访问时检查删除，对内存不友好）、定期删除（周期检查部分键，平衡CPU和内存）；Redis采用惰性删除+定期删除组合方案。

### 8.2 持久化与淘汰

**题目 6**: Redis的持久化机制有哪些？有什么区别？

**答案**: RDB（快照）：指定时间间隔生成数据快照，文件小恢复快，可能丢失最后一次快照后数据；AOF（追加）：记录所有写命令，实时性好但文件大，有三种同步策略（always/everysec/no）；Redis 4.0后支持混合持久化，前半段RDB+后半段AOF。

**题目 7**: Redis的内存淘汰策略有哪些？

**答案**: 内存淘汰策略（maxmemory-policy）：不淘汰noeviction；volatile-lru/ttl/random/lfu从有过期时间的键淘汰；allkeys-lru/random/lfu从所有键淘汰；常用策略：allkeys-lru（LRU算法淘汰最久未使用）、volatile-lru（只淘汰设过期时间中LRU）、noeviction（拒绝写入）。

**题目 8**: 什么是LRU和LFU算法？Redis如何实现？

**答案**: LRU（最近最少使用）淘汰最久未访问的key；LFU（最不经常使用）淘汰访问频率最低的key；Redis 4.0前只用LRU，通过对象头lru字段记录访问时间；4.0后支持LFU，lru字段分为ldt（上次访问时间）和logc（访问频次），logc会随时间衰减。

**题目 9**: Redis的主从复制原理是什么？

**答案**: 主从复制流程：Slave发送PSYNC命令→Master返回BGSAVE+缓冲区→Slave写入RDB文件+同步缓冲区数据；全量复制：Master执行BGSAVE生成RDB，发送给Slave；增量复制：Master将新写命令发送给Slave；哨兵（Sentinel）实现自动故障转移。

### 8.3 缓存与集群

**题目 10**: 什么是缓存击穿、缓存穿透、缓存雪崩？如何解决？

**答案**: 缓存击穿：热点key过期，瞬间大量请求打数据库；解决：热点key永不过期、使用互斥锁限流。缓存穿透：查询不存在的数据绕过缓存直击数据库；解决：布隆过滤器过滤、缓存空值。缓存雪崩：大量key同时过期或Redis宕机；解决：key过期时间加随机值、高可用架构（哨兵/集群）、服务熔断限流。

**题目 11**: 如何保证缓存和数据库的双写一致性？

**答案**: 四种方案：先更新数据库后更新缓存（不建议，并发问题）、先更新缓存后更新数据库（不建议，数据库失败不一致）；推荐方案：先删除缓存后更新数据库（配合延迟双删）、先更新数据库后删除缓存（配合消息队列补偿）；使用分布式锁保证原子性。

**题目 12**: Redis有哪些集群方案？

**答案**: Redis集群方案：主从复制（读写分离，一主多从）；哨兵模式（主从复制+自动故障转移）；Redis Cluster（数据分片，16384个槽位，客户端直连）；Codis/ Twemproxy（代理分片）；各有优缺点，Cluster是官方方案。

**题目 13**: Redis如何实现分布式锁？

**答案**: Redis实现分布式锁：SET key value NX PX timeout（设置唯一值+过期时间）；释放锁时用Lua脚本原子性检查并删除（IF value==myvalue THEN DEL）；需注意：value使用唯一ID防止误删、过期时间要大于业务处理时间、可重入性需维护计数器。

**题目 14**: Redis的事务支持是怎样的？有什么局限性？

**答案**: Redis事务通过MULTI、EXEC、DISCARD、WATCH命令实现；MULTI开启事务，EXEC执行所有命令，WATCH监控键变化实现乐观锁；局限性：不支持回滚（命令错误仍执行其他命令）、不支持rollback、没有隔离级别、无法保证原子性（EXEC前可能其他命令执行）。

**题目 15**: Redis和Memcached有什么区别？如何选择？

**答案**: 数据类型：Memcached只支持字符串，Redis支持多种数据结构；持久化：Redis支持RDB和AOF，Memcached不支持；集群：Redis Cluster原生支持，Memcached需客户端实现分片；内存管理：Redis使用动态内存分配，Memcached更简单；选择：需要复杂数据结构选Redis，简单KV缓存选Memcached。

## 9. AI 辅助开发面试题

### 9.1 基础概念

**题目 1**: LLM是什么？为什么它不能直接访问数据库？

**答案**: LLM（Large Language Model）是大型语言模型，本质是一个"超强文本预测机器"，能够理解、推理、写代码、总结和决策；但它自己不能访问数据库、不能调用接口、不能查实时数据、不能执行程序，只是一个"会思考的大脑"。

**题目 2**: 什么是 RAG？它解决了什么问题？

**答案**: RAG（Retrieval-Augmented Generation）是检索增强生成，让LLM先去"查资料"再回答问题；解决的问题：LLM不知道公司数据库、不知道内部文档、不知道最新数据；RAG通过先检索相关资料再喂给LLM的方式，让AI能够回答基于私有数据的实时问题。

**题目 3**: 什么是 Agent？它与传统后端逻辑有什么区别？

**答案**: Agent是会思考、会调用工具、会执行任务的AI系统；传统后端是写死的if-else逻辑，Agent由LLM自己决定下一步做什么（查数据库、分析数据、生成报告）；核心区别：传统程序是固定的，Agent是动态决策的。

**题目 4**: LangChain和LangGraph是什么？它们在架构中属于什么角色？

**答案**: LangChain是Agent开发框架，帮你调LLM、管理Prompt、管理工具、管理RAG，类似AI时代的Spring Boot；LangGraph是复杂工作流引擎，支持判断、分支、循环、多节点协作，类似状态机和工作流引擎；LangChain偏简单链式调用，LangGraph支持有状态的DAG。

**题目 5**: 什么是 MCP协议？它有什么作用？

**答案**: MCP（Model Context Protocol）是Anthropic提出的协议，让LLM用标准协议访问外部工具；它规定了怎么声明工具、怎么调用、怎么返回结果；可以理解为AI世界的gRPC协议，现在很多IDE和AI Agent都在支持MCP。

### 9.2 Prompt工程

**题目 6**: 什么是Prompt Engineering？为什么它很重要？

**答案**: Prompt Engineering是通过设计输入指令、上下文和输出约束，让大模型按预期稳定完成任务的工程方法；因为LLM的行为高度依赖输入文本，好的Prompt相当于用自然语言编写动态业务逻辑；在实际项目中，Prompt包括角色设定、任务拆解、格式约束和工具调用指令。

**题目 7**: 如何让LLM输出稳定的JSON格式？

**答案**: 三层保险方案：1）Prompt强约束格式（写明"严格按照JSON输出"、给出示例结构）；2）使用模型的JSON Mode或Function Calling模式；3）后端做JSON校验和重试机制；生产环境不能只依赖Prompt，要配合结构化输出和后处理校验。

**题目 8**: 什么是幻觉（Hallucination）？如何通过Prompt减少幻觉？

**答案**: 幻觉是模型编造事实（最大工程问题），比如编造不存在的数据或瞎写接口结果；减少方法：提供真实上下文（RAG思想）、强约束禁止编造（如果信息不足请回答"信息不足"）、工具优先策略（必须先调用工具再回答）、降低开放性（越具体幻觉越少）。

**题目 9**: System Prompt、User Prompt、Context Prompt有什么区别？

**答案**: System Prompt定义AI的"人设"和规则（如"你是一个严谨的后端工程师"）；User Prompt是用户真实输入的问题；Context Prompt是给模型的额外资料（如数据库查询结果），在RAG中非常常见；三者组合构成完整的Prompt。

**题目 10**: 什么是Few-shot Prompt？它有什么效果？

**答案**: Few-shot是给模型示例输入输出，让模型学习模式；效果是大幅提升输出稳定性；示例格式：先给"输入：用户增长100 → 输出：{"trend": "上升"}"，再让模型分析新数据；工程中常用于需要固定输出格式的场景。

### 9.3 函数调用与工具

**题目 11**: 什么是Function Calling？为什么比解析自然语言更稳定？

**答案**: Function Calling是让LLM以结构化方式调用外部工具的机制，模型输出函数名和参数而非自然语言；比解析自然语言更稳定因为：结构化输出可校验、有类型约束、不会出现理解偏差；在生产环境应优先使用Function Calling或JSON Mode。

**题目 12**: Agent如何避免无限调用工具（死循环）？

**答案**: 工程策略：限制最大步骤数（max iterations）、检查重复调用模式、设置超时机制、监控token消耗；Agent系统需要设置最大推理步数，设置工具调用超时，记录reasoning trace，这些是生产级别Agent必须考虑的问题。

**题目 13**: RAG和微调（Fine-tune）有什么区别？什么时候用哪个？

**答案**: RAG通过检索外部知识增强模型输入，不改模型参数，更新成本低，适合企业知识库；微调是修改模型参数让模型长期记住知识或风格，成本高，适合固定风格/任务；企业场景大部分优先用RAG，因为成本低、更新快、风险小。

**题目 14**: Token是什么？为什么需要控制Token消耗？

**答案**: Token是模型处理文本的最小单位，模型的计算成本、上下文长度、价格都和token数量直接相关；控制方法：限制输入长度、文本分块（chunking）、做缓存；生产系统需要控制token消耗和响应延迟。

### 9.4 系统架构

**题目 15**: 智能客服Agent的典型架构是怎样的？

**答案**: 典型架构：用户 → API Gateway → Agent Orchestrator → LLM → Tools（知识库/DB）；核心模块包括Prompt管理、RAG检索、工具调用、日志系统、成本控制、超时机制；稳定性策略：设置最大推理步数、工具调用超时、记录reasoning trace、JSON校验重试。

**题目 16**: LLM系统的最大工程挑战是什么？

**答案**: 最大挑战是不确定性和可控性；具体包括：输出不稳定（需结构化约束）、成本不可预测（需token控制）、幻觉问题（需RAG+约束）、上下文爆炸（需截断+摘要）、延迟问题（需异步+流式）；解决这些需要扎实的后端工程能力。

**题目 17**: temperature参数是什么？怎么调？

**答案**: temperature控制输出随机性，越低越稳定，越高越发散；数据分析或结构化任务中，使用低temperature（0-0.3）保证输出稳定；创意生成场景可以调高；知道什么时候用什么参数说明有实战经验。

**题目 18**: Go后端在AI开发中有什么优势？

**答案**: Agent真正难的不是调API和写Prompt，而是工具设计、并发控制、超时重试、状态管理、成本控制、日志追踪、分布式架构；这些恰恰是Go后端擅长的领域；Go在AI时代的价值在于构建稳定可靠的AI基础设施。



